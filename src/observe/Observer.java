package observe;

import java.beans.PropertyChangeListener;

/**
 * A class can implement the Observer interface
 * when it wants to be informed of changes of a subject.
 *
 * @see Subject
 */
public interface Observer extends PropertyChangeListener {

}
